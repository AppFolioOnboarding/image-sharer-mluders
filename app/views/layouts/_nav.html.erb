<nav class="navbar navbar-expand-lg navbar-light bg-light margin-bottom-50">
  <div class="container">
    <a class="navbar-brand" href="<%= images_path %>">Image Sharer</a>
    <ul class="navbar-nav ml-auto">
      <li class="nav-item">
        <a class="nav-link <% if current_page?(images_path) %> active <% end %>" href="<%= images_path %>">Home</a>
      </li>
      <li class="nav-item">
        <a class="nav-link <% if current_controller_actions?('images', ['new', 'create']) %> active <% end %>" href="<%= new_image_path %>">Upload</a>              
      </li>
    </ul>
  </div>
</nav>