<% if flash[:notice] %>
  <div class="notice"><%= flash[:notice] %></div>
<% end %>


<h1 id="image-title">
  <%= @image.title %>
</h1>

<img src="<%= @image.url %>" alt="<%= @image.title %>" />

<br>

<%= link_to 'Back', images_path %>
